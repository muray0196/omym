[pytest]
markers =
    domain: unit tests focused on feature domain modules.
    usecase: application service tests exercising orchestrations with mocked ports.
    integration: adapter + platform integration tests hitting real infrastructure.
addopts = -ra
